import { ModalBox } from "@/styles/modal";
import styled from "styled-components";

const DeleteModalBox = styled(ModalBox)`
  width: 27.5rem;
  height: 15rem;

  p:first-child {
    color: black;
    margin: 2rem;
  }
`;

export { DeleteModalBox };
