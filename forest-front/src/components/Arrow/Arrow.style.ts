import styled from "styled-components";

const ArrowIcon = styled.span`
  position: relative;
  cursor: pointer;
  font-size: 2rem;
  height: 2rem;
  color: ${({ theme }) => theme.colors.Lime[900]};
`;

export { ArrowIcon };
