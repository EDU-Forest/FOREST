interface SearchWorkbook {
  workbookId: number;
  title: string;
  workbookImgPath: string;
  bookmarkCount: number;
  scrapCount: number;
  methodType: string;
  isScraped: boolean;
  isBookmarked: boolean;
}
